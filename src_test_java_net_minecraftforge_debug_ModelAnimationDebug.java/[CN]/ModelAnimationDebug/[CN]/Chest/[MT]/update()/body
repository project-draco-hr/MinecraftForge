{
  if (opening) {
    tick++;
    if (tick >= maxFrame) {
      tick=maxFrame;
      opening=false;
    }
  }
  if (closing) {
    tick--;
    if (tick <= minFrame) {
      tick=minFrame;
      closing=false;
    }
  }
}
