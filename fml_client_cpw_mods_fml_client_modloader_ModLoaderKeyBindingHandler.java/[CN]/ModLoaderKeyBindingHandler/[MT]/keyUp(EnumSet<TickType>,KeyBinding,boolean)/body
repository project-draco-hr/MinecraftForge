{
  if (!end) {
    return;
  }
  downArmed=false;
  if (type.contains(TickType.CLIENT)) {
    upArmed=true;
  }
  if (type.contains(TickType.RENDER) && upArmed) {
    onRenderEndTick();
    upArmed=false;
  }
}
