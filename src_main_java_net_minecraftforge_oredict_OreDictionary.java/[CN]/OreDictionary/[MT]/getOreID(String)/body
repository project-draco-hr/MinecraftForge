{
  Integer val=nameToId.get(name);
  if (val == null) {
    idToName.add(name);
    val=idToName.size() - 1;
    nameToId.put(name,val);
    idToStack.add(new ArrayList<ItemStack>());
  }
  return val;
}
