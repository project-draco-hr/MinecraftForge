{
  setup();
  Class cls=this.getClass();
  boolean found=false;
  while (cls != Event.class) {
    if (cls.isAnnotationPresent(Cancelable.class)) {
      found=true;
      break;
    }
  }
  isCancelable=found;
}
